1. The Scaling Rules Agent has been deployed.
2. It reuses the shared credentials secret configured by the parent chart.
3. Verify connectivity to the scaling rules API through the configured `API_URL` value.
